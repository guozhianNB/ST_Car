# P60 重新调整后的安全行程记录（2026-07-31）

## 结论

- `VERIFIED（操作者确认）`：摆杆机械结构重新调整后，以新机械中点为零，两侧安全行程至少各为 `550 count`。
- 正式软件边界采用 `[-500,+500] count`，两侧至少保留 50 count 余量。
- 调整前的 `[-320,+320] count` 不再适用于当前机械结构。

## 条件与方法

- 日期：2026-07-31。
- 单位：TIM4 编码器 TI12 模式最终计数（count），软件不再额外乘四。
- 方法：操作者完成机械结构重新调整，并口头确认从新机械中点向正、负两侧至少各有 550 count 安全范围。
- 原始逐点计数、电流和两侧机械硬止点数据未保存，因此不得据此把软件边界继续放宽到超过 ±500。

## 使用与恢复

1. 每次上电必须先把曲柄对准新机械中点，再建立 P60 零点。
2. 首次闭环记录目标角、SA100 角度、P60 计数、PWM 与惯性峰值。
3. 若出现干涉、死点、异常电流或超过 ±500，立即停止；不得绕过方向限位。
4. 如新结构表现与操作者确认不符，恢复调整前 `[-320,+320]` 并重新测量两侧边界。
