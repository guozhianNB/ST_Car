# ST_Car 项目上下文与开发约定

本文是本仓库的项目事实入口，供后续 agent 和开发者在修改 STM32、MaixCAM2、CubeMX 配置或控制算法前阅读。

## 1. 项目身份与事实优先级

本项目对应《2026 年全国大学生电子设计竞赛赛区赛（TI 杯）暨模拟电子系统设计专题赛选拔赛》H 题“车载平衡滚球运动控制系统”。原题位于：

- `docs/H题_车载平衡滚球运动控制系统.pdf`

出现冲突时按以下优先级处理：

1. 原题 PDF 中的比赛硬约束；
2. 已记录的实物测量和标定结果；
3. 本文中已确认的系统方案与开发约定；
4. 当前源码和源码注释；
5. 尚未实测的估计值。

不得把估计参数、计划模块或占位代码写成“已完成”。参数未实测时必须保留 `TODO/UNVERIFIED` 标记。

## 2. 原题硬约束（已从 PDF 核对）

### 2.1 场地与整车

- 小车沿黑色环形线顺时针行驶。
- AB、CD 为各 1.5 m 的直线，BC、DA 为半径 0.5 m 的半圆弧。
- 黑线宽度为 `1.8 ± 0.2 cm`。
- A 点有一条与环路垂直、长度 5 cm、线宽 `1.8 ± 0.2 cm` 的居中启停线；其中心轴上另有宽 0.1 cm、长 30 cm 的停车基准虚线。
- 车身外廓不得超过 `35 cm（长）× 25 cm（宽）`；摆杆也不得超出车身。
- 小车必须轮式驱动并由车载电池供电。
- 必须在小车中心轴线上明确一个唯一测试位置，起点和停车偏差都以此位置判断。
- 行驶中不得人工干涉或遥控。小车投影必须保持在轨迹线上；投影完全脱离轨迹线即测试失败。
- 循迹只能使用红外光电模块，数量不限。
- 必须有启动按键和显示装置。按键启动时开始计时并显示时间；显示屏不大于 2 英寸并安装在易观察位置。

### 2.2 摆杆、钢球与图传

- 摆杆由 25 cm 长的 4 分 PPR 水管改造：外径约 2 cm、壁厚约 0.34 cm。
- 槽内钢球直径约 1 cm；摆杆截面外圆弧半径约 1 cm、内圆弧半径约 0.65 cm。
- 摆杆必须平直，凹槽内壁光滑并保持原状；不得制造凹坑或进行任何增摩擦改造。
- 刻度只能贴在凹槽剖面边沿，不能贴入槽内，刻度间距 0.1 cm。
- 摆杆左端用铰链或合页固定，距小车平板高度 `h ≥ 5 cm`。
- 凹槽内钢球位置检测必须使用摄像头。
- 图传发送模块必须稳固安装在车上，画面覆盖整个摆杆并能看清钢球轨迹、判断位置；接收端位于环路外。
- 每次测试的视频必须完整记录且可回放。接收模块及其显示/存储设备比赛结束后随作品封存。
- 控制视觉和比赛图传可以是不同链路；不要因为 MaixCAM2 能测球位就遗漏比赛要求的实时显示、录像和回放。

### 2.3 测试项目

1. 图传与录像：稳定实时显示钢球运动并完整记录、回放，6 分。
2. 单独循迹：从 A 按键启动，顺时针一圈并停在 A；总时间 `≤20 s`，停车偏差 `≤2 cm`，16 分。
3. 静态滚球：小车静止，球从 O 到 `+5 cm`，到达后折返至 `-5 cm` 并稳定；全过程 `≤5 s`，在两个目标处的最大误差绝对值 `≤1 cm`，13 分。
4. 行驶中心平衡（AB）：从 A、球初始在 O，顺时针通过 B；AB 时间 `≤8 s`，行驶中球相对 O 的误差绝对值 `≤1 cm`，20 分。
5. 行驶中心平衡（整圈）：从 A、球初始在 O，顺时针一圈并通过 A；总时间 `≤30 s`，球误差绝对值 `≤1 cm`，20 分。原题要求“通过 A”，并未要求本项停车。
6. 任意位置平衡（整圈）：球先放在摆杆上的任意指定位置，小车从 A 顺时针一圈并通过 A；总时间 `≤30 s`，球相对指定位置的误差绝对值 `≤1 cm`，20 分。目标位置是测试前指定，不应默认成运行中先搬运球到目标。

## 3. 已确认的总体方案

总体架构可行：NUCLEO-G491RE 负责所有实时控制，MaixCAM2 负责钢球视觉定位并通过 UART 上报，比赛图传使用校队统一方案。前期不引入 RTOS，采用 HAL、定时任务、中断/DMA 和非阻塞状态机。

控制分为三个可独立调试、由总状态机协调的部分：

1. 左右轮独立轮速 PI 内环；
2. 红外循迹 PD/PID 外环，输出左右轮目标速度；
3. 钢球位置外环，输出水管目标倾角；SA100 水管真实角度内环驱动 P60。

数据流为：

```text
x_ref -> 球位置外环 -> theta_ref -> SA100 角度内环
      -> P60/TB6612 -> 曲柄连杆 -> 水管倾角 -> 球运动 -> MaixCAM2 -> x_ball

line_error -> 循迹控制 -> v_left_ref/v_right_ref
           -> 左右轮独立速度 PI -> 两个 P30/TB6612 -> 小车运动
```

## 4. 硬件方案与暂定参数

### 4.1 底盘

- 双轮差速，左右驱动轮为 MG513P30，轮径 66 mm，均带 AB 相编码器。
- 当前暂定输出轴每圈计数：P30 `13×30=390`，P60 `13×60=780`。这两个值尚未确认是否已经包含当前定时器编码模式的倍频，必须实测一整圈。
- 轮距尚未测量；`Core/Inc/app_config.h` 当前填写 `180.0 mm` 只是 `UNVERIFIED` 占位，使用前必须用最终轮胎接地点中心距替换。
- 两个 P30 由 TB6612 驱动，11.1 V 航模电池供电。
- NUCLEO、MaixCAM2、所有 TB6612 和电源地必须共地。
- 软件和接线合同规定底盘/P60 两块 TB6612 的 `STBY` 分别由 PB0/PB1 控制且复位默认低；实物板若带固定上拉或焊接跳线，必须在接电机前确认并处理，不能假设软件一定能关断。

### 4.2 摆杆机构

- 左侧支点安装 SA100，SA100 外壳固定于车架，轴随水管转动，直接测量水管真实倾角。
- 左支点到右侧连杆连接点约 250 mm。
- 右侧 MG513P60 通过 TB6612 驱动曲柄连杆；曲柄半径约 20 mm，连杆长约 77 mm。
- 理论工作摆角约 `±4.6°`。初调软件限幅使用 `±2°`，机构和内环稳定后才逐步放宽。
- P60 编码器只用于安全工作区、堵转、方向和调试，不能代替 SA100 的水管角度反馈。
- 不需要在 P60 轴上再安装第二个 SA100。
- 曲柄接近死点时传动关系恶化，P60 编码器正负安全限位是强制保护，不得绕过。

P60 编码器是增量反馈：断电后中点会丢失。每次上电必须通过人工中位确认、可靠归零/限位流程或后续新增的绝对参考重新建立 `p60_encoder_zero`，不得直接沿用上次 RAM 中的零点。

### 4.3 MaixCAM2

- 摄像头通过足够刚的支架随水管运动，使其与水管相对位姿近似固定。
- 当前标定点为图像左侧 `-10 cm`、右侧 `+10 cm`。
- 正式算法应把球心投影到两标定点定义的水管轴向量上，再换算一维毫米坐标；不能长期只使用图像横坐标。
- 安装方向改变、分辨率改变、相机移动或重新对焦后必须重做标定。

## 5. 控制与安全约定

### 5.1 单位和坐标

- 长度统一为 mm，速度统一为 mm/s，时间统一为 ms。
- 固件内部角度统一使用“度”；若某个数学函数要求弧度，只允许在调用边界显式转换并在变量名中体现。
- 当前视觉约定为图像左标记 `-100 mm`、右标记 `+100 mm`。装车后必须验证 `x_ball` 正方向、SA100 正角方向和 P60 正输出方向，统一写入参数，控制器中不得散落临时负号。

### 5.2 调度频率目标

- 1 ms：系统时基、快照/事件标志；高成本安全判断可分频执行。
- 2–5 ms（200–500 Hz）：水管角度内环，最终频率受 SA100 有效更新率限制。
- 5–10 ms（100–200 Hz）：左右轮速度环。
- 5–10 ms：循迹外环。
- 每个新视觉测量帧（约 25–50 Hz）：球位置外环；帧间保持上一 `theta_ref`。
- 10–20 ms：总状态机。
- 50–100 ms：显示与遥测。
- 电机 PWM 目标约 20 kHz。所有控制任务非阻塞，不在运行态使用长 `HAL_Delay()`。

### 5.3 控制器原则

- 左右轮必须各自有物理速度目标和独立 PI；循迹只修改两轮目标速度，不能把“左右速度差校正 + 开环 PWM”误称为轮速闭环。
- 速度目标必须带斜坡/加速度限制，停车也应分阶段减速，禁止突然反转。
- 循迹误差大或进入弯道时降低基础速度；直线才逐步提高速度。
- 球位置外环先用 PD：`theta_position = Kp*e_x - Kd*v_ball`，确认符号后再考虑小积分。
- 水管角度内环先用 PD 或小积分 PID，必须含输出限幅、抗积分饱和、死区补偿、角度软件限位和 P60 编码器方向限位。
- 小车加速度前馈接口保留但默认关闭：`theta_ff ≈ atan(a_car/g)`；实车验证符号和延迟后再启用。
- 到达目标必须采用持续时间判据，例如 `|error|≤10 mm` 连续 200–300 ms；不能用单帧命中。
- 视觉导出的球速必须使用真实帧间隔，并对异常值与噪声做滤波；不要对“保持上一位置”的伪测量计算速度。

### 5.4 安全状态不是单一动作

至少区分：

- 可恢复故障（短时视觉丢球等）：底盘减速，角度反馈仍可信时把目标倾角平滑收回 0°。
- 角度传感器失效、编码器状态不可信、严重越界或驱动异常：立即禁止危险方向或关闭输出，不能盲目执行“慢慢回水平”。
- 到达 P60 软件边界时，只允许向安全区退回的输出。
- 上电、复位和模式切换默认三电机输出为零；只有完成必要传感器检查并收到明确启动事件后才允许运动。

## 6. 状态机目标

建议状态至少包括：

- `BOOT/SELF_CHECK`：初始化并检查传感器、通信、归零状态；电机关闭。
- `IDLE`：选择模式和目标，等待启动按键。
- `MANUAL_DEBUG`：逐个电机、编码器、SA100、视觉和循迹输入调试，带硬限幅。
- `ANGLE_TEST`：依次跟踪 `0,+1,-1,+2,-2,0°`，不放球。
- `STATIC_BALL`：O -> +50 mm -> -50 mm，持续判稳并记录总时间。
- `LINE_ONLY`：一圈后识别 A 横线、减速并停车。
- `BALANCE_AB`：中心平衡并通过 B，确保 AB `≤8 s`。
- `BALANCE_LAP`：中心平衡整圈，通过 A 时结束计时；不默认停车。
- `BALANCE_ARBITRARY`：预设任意目标并整圈保持，通过 A 时结束计时。
- `FAULT`：根据故障等级执行受控停止或立即关断，并锁存原因。

A 点识别必须防止启动即把起点横线当终点：先确认离开 A（普通纵线状态 + 最小里程/时间），之后才允许再次识别横线。横线检测还应有去抖和编码器里程佐证。

## 7. STM32 目标模块边界

`main.c` 只做 CubeMX 初始化、软件模块初始化和调度，不直接容纳 PID、协议解析或比赛流程。

目标模块：

- 驱动：`motor_tb6612`、`encoder`、`sa100`、`vision_uart`、`line_sensor`、`button`、`display`、`timebase`。
- 算法：`pid`、`filter`、`wheel_speed_control`、`line_follow_control`、`beam_angle_control`、`ball_position_control`、`chassis_feedforward`。
- 应用：`system_state`、`task_modes`、`safety_monitor`、`parameters`、`telemetry`。

优先使用清晰的实例结构体或电机 ID 统一三个 TB6612 通道，避免复制三套硬编码函数。所有可调值集中在参数模块；参数至少包括每圈计数、轮径、轮距、方向、零点、限位、PWM 死区/上限、循迹权重、速度/加速度、PID、超时和丢球策略。

业务逻辑不得写入 CubeMX 外设初始化文件；外设和引脚优先在 `ST_Car.ioc` 中配置。当前部分 HAL 初始化文件经过手工整理，不应无审查地重新生成。重新生成前先保存/提交改动，生成后逐项检查 diff，并把业务逻辑继续保留在独立模块中。

在扩展外设前必须先做完整的引脚/定时器资源表。至少需要：3 路 PWM、3 组 AB 编码器、6 路方向控制、1 路 SA100 输入捕获、红外循迹输入、MaixCAM2 UART、显示接口、启动/模式按键和调试串口。

## 8. MaixCAM2 串口协议

当前 `Vision/cv.py` 实际发送：

```text
$B,<x_mm>,<status>\n
```

- 波特率 115200，MaixCAM2 UART4，TX=A21、RX=A22。
- `status=1`：本帧真实检测到球。
- `status=2`：短时丢失，沿用滤波后的上一位置；这是预测/保持值，不是有效新测量。
- `status=0`：无可靠球位，发送 `0.0` 占位。

STM32 解析器必须按换行组帧、限制长度、校验字段和数值范围，并按接收时间实施超时。只有 `status=1` 才能更新测量时间、位置滤波和球速；`status=2` 最多用于短时保持，不能当作新采样。

后续协议建议升级为包含 `frame_id`、质量分数/置信度和可选采集时间戳的版本；若改协议，必须同步修改两端并在本文记录版本。二进制协议不是前期必需，文本协议足以调试，但必须避免阻塞接收。

## 9. 当前仓库真实状态（2026-07-29 固件重构后）

### 9.1 已实现的软件框架

- `ST_Car.ioc` 已完整配置 NUCLEO-G491RE / STM32G491RET6、HSI+PLL 170 MHz、3 路 20 kHz PWM、3 组 AB 编码器、SA100 PWM Input、UART4 环形 DMA、LPUART1、I2C1/I2C3、8 路循迹 GPIO 和 3 个 EXTI 输入。
- `Core/Src/main.c` 仅做外设初始化并运行无阻塞 `App_Run()`；复位后两块 TB6612 的 STBY 均保持低，必须通过启动检查才会使能。
- `motor`/`encoder` 已统一三个电机，正确区分 TIM2 32 位与 TIM3/TIM4 16 位回绕，左右轮输出真实 mm/s。
- `sa100` 已实现 1 us PWM Input 捕获、周期/高电平校验、占空比角度、跨 0° 展开和超时。
- `vision_uart` 已实现 UART4 RX DMA+IDLE、限长换行组帧、数值范围检查和球速低通；只有 `status=1` 更新位置、测量时间和球速，`status=2` 不会污染微分。
- `line_sensor` 已实现 8 路有序采样、加权误差、丢线保持和 A 点横线判断。
- `pid`/`control_loops` 已实现左右轮独立 PI、目标速度斜坡、循迹 PD/弯道降速、球位置 PD 和 SA100 水管角度 PID，并强制 P60 方向限位。
- `app`/`safety_monitor` 已实现待机、单独循迹、静态滚球、AB 中心平衡、整圈中心平衡、任意位置平衡、角度测试、完成和故障状态。
- `telemetry` 每 100 ms 通过 ST-Link VCP 非阻塞输出控制快照。
- `EWARM/ST_Car.ewp` 已纳入当前模块和必要 HAL 驱动；核心源文件已通过 GCC `-Wall -Wextra -Werror` 语法检查及完整 Cortex-M4 链接。
- `Vision/cv.py` 保留 640×480、30 FPS 的钢球检测和毫米位置发送实现。

详细接线见 `docs/stm32-pinout.md`，代码分层、状态机、故障行为和继续调试方法见 `docs/firmware-architecture.md`。

### 9.2 仍需实物完成或补齐

- 当前参数均为有保护的初始值，三个电机方向、三个编码器方向/每圈计数、最小 PWM、轮距、P60 边界和所有 PID 都尚未实测调好。
- SA100 的 `duty * 360°`、水平原始角 `180°` 和方向只是占位；拿到传感器公式或实测拟合后必须更新 `app_config.h`。
- AB 模式当前用平均里程 1550 mm 判断“已通过 B”；需在赛道上校准。若增加 B 点专用标志/传感器，必须明确合规性后再改。
- 底盘纵向加速度前馈尚未加入；基础双环稳定前保持关闭是有意设计。
- OLED 型号尚未最终确认。I2C1 PB8/PB9 已预留，但旧 `oled.*` 缺字体依赖且没有加入工程；正式显示适配层仍需在硬件确定后完成。
- 缺少人工调试命令行/屏幕菜单；当前通过模式键、启动键、编译期参数和 LPUART1 遥测完成初调。
- 代码已交叉编译和链接，但尚未在真实 NUCLEO、电机、SA100、循迹板和 MaixCAM2 上做硬件在环验证。

### 9.3 上车前强制检查

- 第一次烧录必须让车轮悬空、连杆脱开，将两路 STBY 与 MCU 控制线隔离并在驱动侧可靠下拉到 GND；先用示波器确认 PA8/PA9/PA10 均为约 20 kHz 且复位占空比为 0。
- 确认 NUCLEO 板载 PC13 按钮是内部下拉、上升沿；PA4 模式键和 PA15 中点开关为内部上拉、对地下降沿。
- P60 每次启动摆杆模式时必须位于机械安全中点；SA100 完成有效公式、零点和方向标定后，还应确认其显示水平约 ±0.5°。两项启动检查都通过后才把增量编码器清零。
- 默认 P60 范围 `[-260,+260]`、水管限位 ±3° 只是保守占位，必须在脱开钢球、低 PWM 条件下测量后缩小或确认，禁止直接放宽。
- `status=2` 是视觉保持值，不是测量。修改 `vision_uart` 时不得让它刷新 `timestamp_ms` 或球速。
- 旧 `pid_control.*`、`i2c_soft.*`、`oled.*`、`road_1.c` 未加入 IAR 工程，不应重新与新控制链混编。
- CubeMX 重新生成前必须先保存改动，生成后逐项核对 `docs/stm32-pinout.md`；特别防止 STBY 默认高、UART4 DMA 丢失或 TIM1/TIM4 功能被换回旧配置。

### 9.4 视觉端已知限制

- 当前毫米换算函数只使用像素 x；参考轴线虽用于 y 约束，却没有用向量投影计算轴向位置。
- 当前协议没有帧号、质量值、时间戳或校验。
- `status=2` 是保持值；若 STM32 把它当真实新帧，会错误估算零速度或污染微分项。
- 标定常数和 LAB 阈值来自当前画面，换光照、相机位姿或结构后必须复测。
- 视觉与 STM32 的坐标正负号必须随最终安装实测；只允许在集中参数中修正方向。

## 10. 推荐实物验收顺序与阶段门禁

固件框架、上电零输出和 20 kHz PWM 已完成静态/编译验证，但以下每一级仍需实物通过；不得因为代码存在而跳级：

1. 将两路 STBY 在驱动侧硬件保持低，确认电源极性、稳压值、共地、复位电平和三路 PWM 波形。
2. 每次只接一个电机并悬空测试方向、制动/滑行语义、最小有效 PWM、空载电流和最大安全 PWM。
3. 手动转动每个输出轴，确认编码器 A/B 电平、方向、回绕和最终 TI12 模式下一整圈计数。
4. P60 先脱开连杆或保证曲柄远离死点，以很小 PWM 找到安全中点和两侧机械边界；软件边界必须比机械边界留出余量。
5. 单独接入 SA100，记录周期/高电平/占空比，确认厂家换算公式、水平零点、方向、抖动和拔线超时。
6. 不放钢球，完成水管 `0,+1,-1,+2,-2,0°` 角度跟踪；验收前不得增大 ±2° 初始目标限制。
7. 固定 MaixCAM2，升级轴向投影并验证 `-100,0,+100 mm` 标定、端到端延迟、丢球状态和 UART 超时。
8. 小车静止完成球位置外环，再完成 O -> +50 -> -50 mm 的 5 s 项。
9. 分别调通左右轮 mm/s PI、速度斜坡和里程；确认两轮都能独立跟踪目标后再开循迹。
10. 调红外循迹、弯道降速、离开 A/再次横线识别和精确停车。
11. 以最低可控速度组合中心平衡与循迹，保存日志后逐步提高速度。
12. 最后调任意位置模式；纵向加速度前馈必须在无前馈的完整系统稳定后再启用。

每个阶段都保存可复现实验条件和日志：参数版本、电池电压、机械配置、目标/反馈/输出、周期抖动、用时、最大误差和故障原因。没有数据不要宣称“已调好”。

## 11. 后续必须补齐的实测/决策

- `wheel_track_mm`；
- 三个电机最终编码模式下的 counts/rev 与方向；
- 三路电机最小有效 PWM、上限和制动/滑行接线语义；
- P60 每次上电的归零方式、机械中点和正负安全限位；
- SA100 PWM 周期/占空比到角度的厂家公式或实测拟合、水平零点和方向；
- 红外模块最终选 2/4/8 路、数字还是模拟输出、权重与横线判定；
- 显示屏型号、尺寸与 I2C/SPI 接口；
- NUCLEO PC10/PC11 与 MaixCAM2 A21/A22 的实物交叉接线、共地和电平验证；
- 第二块 TB6612 的通道分配及全部 `STBY` 接法；
- 小车唯一测试位置的机械标记；
- 电池、电机电源、逻辑电源的分配、降压和共地方案。

这些信息一旦实测，应立即更新本文或集中参数文档，不要只留在聊天记录或个人记忆中。

## 12. 供电、电平与抗干扰边界

### 12.1 TB6612 与电机电源

以下数据来自东芝 TB6612FNG 官方数据手册，是芯片边界，不代表具体模块、PCB 铜箔、接插件和散热一定能达到同样能力：

- 逻辑电源 `VCC` 的工作范围是 `2.7~5.5 V`，电机电源 `VM` 的工作范围是 `2.5~13.5 V`；PWM 输入频率上限为 `100 kHz`。当前固件约 `20 kHz`，位于芯片允许范围内。
- 数据手册给出的工作输出电流上限是每通道 `1.0 A`（`VM >= 4.5 V` 条件）；更高的绝对最大值或脉冲值不能当作连续设计电流。必须实测电机启动、堵转和机构卡住时的电流，并同时受电机、驱动模块、线束、接插件和电池能力约束。
- 如果“11.1 V 航模电池”实际为 3 串锂电池，其满电端电压通常为 `12.6 V`。这仍低于芯片 `13.5 V` 工作上限，但需要给电机反电动势、线束尖峰和模块差异留余量；不得只按标称 11.1 V 判断安全。
- `VM` 只能进入电机驱动电源端，绝不能接到 NUCLEO 的 `5V/3V3` 或任何 GPIO。NUCLEO、MaixCAM2 和驱动板需要共地，但大电流电机回流不应穿过 MCU/传感器的细地线或面包板地线。
- 推荐 TB6612 逻辑 `VCC` 使用 `3.3 V`，与 STM32 GPIO 电平一致。TB6612 的高电平门限为 `0.7*VCC`；若逻辑 VCC 使用 5 V，则保证高电平需要至少 3.5 V，STM32 的 3.3 V 输出不满足数据手册保证值。除非模块自带并已确认电平转换，否则不要用 5 V 逻辑供电直接接 STM32。
- `STBY` 低电平为待机，芯片内部标称带 `200 kΩ` 下拉。长线、模块附加上拉或噪声环境下，可在确认模块原理图后增加更强的外部下拉，使 MCU 复位、未供电或引脚高阻时驱动仍可靠关闭；软件启动时 PB0/PB1 也必须先输出低，再配置 PWM 和方向。

驱动板附近建议在 `VM-GND` 放置与负载相称的储能电容，并在 `VCC-GND`、`VM-GND` 近芯片处保留陶瓷去耦；具体电容量以模块原理图、示波器测得的压降/尖峰和电机电流确定，不在本文虚构固定值。电机线尽量短并成对绞合，编码器、SA100、I2C 和 UART 线远离电机端子与 PWM 大电流回路。

### 12.2 NUCLEO、传感器与通信电平

- 不得笼统认为 STM32G491RE“所有 IO 都耐 5 V”。每个引脚是否为 FT/TT 类型、在 MCU 未上电时能否承受外部电压，都必须按 STM32G491RE 数据手册的引脚表和注释判断。
- SA100 的准确型号、供电电压、PWM 输出幅度和输出级目前均未得到可靠手册确认。接 PB14 前先用示波器测量；若高电平可能超过该引脚允许输入范围，必须使用合适的分压、缓冲或电平转换。不能依靠串联电阻把过压输入变成“安全”。
- MaixCAM2 UART4 使用 A21/TX、A22/RX；与 STM32 交叉连接：`A21/TX -> PC11/UART4_RX`，`A22/RX <- PC10/UART4_TX`，并连接信号地。官方文档提醒，某些会把 TX 拉低的电平转换器可能导致 MaixCAM2 无法启动，因此转换器必须验证静态电平和上电行为。
- MaixCAM2、显示屏和传感器应由满足各自规格的稳压电源供电，GPIO 只能传输信号，不能作为电源输出替代稳压器。供电容量需要覆盖摄像头峰值负载，避免电机启动造成复位或 UART 乱码。
- I2C1/I2C3 必须确认总线是否已有上拉以及上拉电压。当前固件是 `400 kHz`；总线电容、线长或模块电平转换导致波形不合格时先降速验证，不要直接反复复位外设。上拉阻值由供电、总线电容和器件灌电流能力计算/测量，常见值只能作为起点，不能当作本项目已确认值。

## 13. 实物接线合同与首次接电顺序

`docs/stm32-pinout.md` 是当前固件的引脚合同；接线表、`ST_Car.ioc`、GPIO 初始化代码和该文档必须保持一致。任何改针脚操作都要先检查定时器复用、调试接口、板载资源和已有线束，不允许仅改一处宏定义。

首次接电或大改线束后按以下顺序执行：

1. 断开电池、USB 和所有外部供电，逐线核对极性、共地、短路、TB6612 `VM/VCC/STBY/AIN/BIN/PWMA/PWMB`，以及编码器/传感器电源方向。
2. 暂不接电机，P60 连杆脱开，车轮悬空；用万用表确认电源正负之间没有异常低阻。不要用蜂鸣档一次读数替代对电容充电现象的判断。
3. 只给 NUCLEO 逻辑供电，确认 PB0/PB1 为低、三路 PWM 占空比为零、方向脚处于安全状态。PC13 板载启动键当前按“按下为高”处理；PA4 模式键和 PA15 中点开关按“触发为低”处理。
4. 逐个连接编码器、SA100、循迹板、OLED/IMU 和 MaixCAM2 信号，先验证电平和数据，再连接驱动板逻辑电源。外设独立供电时尤其要先建立可靠信号地。
5. 最后接 TB6612 的 `VM`，保持 STBY 低；每次只使能一块板、一个通道、一个电机，以受限 PWM 和可立即断电的方式验证。
6. 每新增一个通道，都重复检查静态电流、5 V/3.3 V 电压、MCU 是否复位、信号波形和电机/驱动温升；异常时先断 VM，再保存现场数据。

除非器件手册明确支持热插拔，否则电机电源存在时不要插拔编码器、SA100、UART、I2C 或电机端子。改变机械连杆、曲柄位置或电机极性前必须断开 VM，并防止水管或曲柄依靠重力突然运动。

## 14. 分阶段硬件验收表

| 阶段 | 必须记录 | 通过条件（未写数值处以手册/实测安全边界为准） |
| --- | --- | --- |
| 逻辑上电 | 3.3 V/5 V、PB0/PB1、PA8/PA9/PA10 波形、复位原因 | 电压稳定；STBY 低；三路 PWM 约 20 kHz 且启动占空比为零；无异常复位 |
| 单电机开环 | 电池电压、PWM、方向、空载电流、启动 PWM、温升 | 正反转和停止语义一致；没有过流、异常噪声或快速升温；急停有效 |
| 单编码器 | A/B 波形、方向、三次整圈计数、回零误差 | 波形电平合规且无明显毛刺；三次计数一致；软件方向与物理正方向约定一致 |
| P60 机构 | 机械中点、两侧机械边界、软件边界、各位置电流 | 软件边界位于机械危险区之前并留有可重复余量；任一边界只允许向安全区退回 |
| SA100 | 周期、高电平、占空比、水平原始值、正方向、拔线行为 | 换算依据可追溯；水平附近稳定；跨零逻辑正确；异常或拔线能在超时后禁止角度闭环 |
| 水管角度环 | 目标/反馈/PWM/P60 计数/周期、峰值误差 | `0,+1,-1,+2,-2,0°` 都能有界跟踪；无持续振荡；换向不过度冲击；保护全程有效 |
| 视觉链路 | 像素、投影坐标、`x_mm`、status、帧间隔、UART 超时 | `-100/0/+100 mm` 和中间点方向正确、重复性稳定；误差应显著小于比赛 10 mm 限值；丢球不生成伪速度 |
| 轮速闭环 | 左右目标/实际速度、PWM、周期、里程 | 两轮分别闭环且能独立跟踪；低速无长期积分饱和；斜坡和制动方向正确 |
| 循迹/整车 | 原始位图、line error、基础速度、差速、A 点事件 | 能离开起点后再识别 A；丢线和横线判定可复现；先低速通过再逐步提高 |

“通过”必须有一份包含固件提交号、参数和原始日志的记录。只观察到一次成功、只看串口最终值或只凭手感，均不能替代验收。

## 15. 常见硬件故障的定位顺序

| 现象 | 优先检查 | 禁止的掩盖方式 |
| --- | --- | --- |
| 电机启动时 NUCLEO/MaixCAM2 复位 | 分别示波 3.3 V、5 V、VM；查大电流回流、稳压容量、接插件压降、反电动势尖峰和复位标志 | 反复自动重启或仅增加软件延时 |
| 上电后电机立即动作 | 物理断开 VM；查 STBY 上拉/跳线、GPIO 复位态、方向和 PWM 引脚波形 | 依赖 `main()` 稍后把 PWM 写零 |
| 有 PWM 但电机不转 | STBY、VM/VCC、电机端电压、方向真值表、通道接线、电流限制、机械卡滞 | 直接不断增大 PWM 上限 |
| 编码器跳数或方向反复 | 示波 A/B 相位与电平，查供电、上拉、接地、线束串扰、定时器模式和溢出处理 | 在控制器里临时取绝对值或堆叠负号 |
| 左右轮同目标但速度不同 | 单独确认 counts/rev、轮径、机械阻力、电池压降和两路 PI，再看负载 | 用循迹差速常量长期补偿硬件/标定错误 |
| 水管角度一闭环就跑向限位 | 立即切 STBY；分别验证 SA100 正方向、目标误差符号、P60 正输出方向和编码器边界 | 先加大 D 或降低 Kp 试图掩盖正反馈 |
| SA100 角度突跳/无效 | 查 PB14 实际周期、高电平、电压范围、跨零和超时；断开传感器验证故障路径 | 用最后角度无限期维持闭环 |
| P60 堵转或边界误报 | 先查曲柄死点、连杆干涉、编码器方向/CPR、计数回绕和堵转判据，再看 PWM | 放宽边界或关闭堵转保护继续运行 |
| MaixCAM2 无 UART 数据 | 查 A21/A22 交叉、共地、115200、UART4 引脚、启动日志、TX 是否被转换器拉低 | 在主控制循环阻塞等待串口 |
| I2C 设备偶发掉线 | 查上拉电压/阻值、地址、线长、波形、供电与总线被拉低；用低速隔离验证 | 在高频控制环中无限重试或长延时 |
| 循迹输入全同或抖动 | 查模块供电/输出类型/极性、离地高度、环境光、阈值和每路原始 GPIO | 直接改 PD 参数或把传感器位图写死 |

任何异常都先把系统拆回最近一次已经验收的层级。定位时一次只改变一个变量，并保存异常发生前后的原始输入、控制输出、电源波形和故障码。

## 16. 标定与实验记录模板

建议每次会改变固件参数的实测都在 `docs/` 下建立日期化记录，至少包含：

```text
日期/操作者：
固件 commit：
硬件版本与接线变更：
电池类型、静置电压、负载电压：
测试阶段与安全措施：
采样周期/日志版本：

P30 left counts/rev、方向、最小有效 PWM、空载电流：
P30 right counts/rev、方向、最小有效 PWM、空载电流：
P60 counts/rev、方向、中点、软件边界、最小有效 PWM：
wheel diameter / wheel track：
SA100 型号、供电、PWM 周期、换算式、水平原始值、方向：
视觉分辨率、安装位置、标定点、拟合参数、残差：
PID/滤波/斜坡参数：
结果、最大误差、稳定时间、故障码：
结论（VERIFIED / FAILED / 仍需复测）：
```

实测值写入 `app_config.h` 时，在提交说明或记录中链接对应实验。若更换电机、轮胎、减速箱、传感器、驱动板、相机安装或机械尺寸，应主动把受影响参数恢复为 `UNVERIFIED`，不能沿用旧车标定。

## 17. 官方资料与仍需保留的未知项

已用于核对本项目硬件边界的官方资料：

- [STM32G491xC/xE 数据手册](https://www.st.com/resource/en/datasheet/stm32g491re.pdf)
- [NUCLEO-G491RE 产品页](https://www.st.com/en/evaluation-tools/nucleo-g491re.html) 与 [STM32G4 Nucleo-64 用户手册 UM2505](https://www.st.com/resource/en/user_manual/dm00556337-stm32g4-nucleo-64-boards-mb1367-stmicroelectronics.pdf)
- [TB6612FNG 官方数据手册](https://toshiba.semicon-storage.com/info/TB6612FNG_datasheet_en_20141001.pdf?did=10660&prodName=TB6612FNG)
- [MaixCAM2 硬件说明](https://wiki.sipeed.com/maixcam2) 与 [MaixPy UART 文档](https://wiki.sipeed.com/maixpy/doc/en/peripheral/uart.html)

仍然缺少可唯一对应实物的 SA100 手册、MG513P30/P60 电机电流/编码器版本资料、TB6612 模块原理图、循迹模块型号与输出电路、显示屏型号、电源模块规格。获得铭牌、购买链接、原理图或数据手册前，这些项目必须保持 `UNVERIFIED`；禁止根据同名商品页面或相似照片猜测电压、线序、分辨率、输出类型或额定电流。
