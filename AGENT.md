# ST_Car 项目事实与当前状态

最后更新：2026-08-01。本文件是项目事实入口；工作规则见 `AGENTS.md`，引脚合同见 `docs/stm32-pinout.md`，控制边界见 `docs/firmware-architecture.md`。

## 1. 目标与比赛约束

项目使用 NUCLEO-G491RE / STM32G491RET6 完成循迹与透明水管滚球任务。题目 PDF 是最高优先级硬约束。当前实现覆盖：单独循迹、静止 `O -> +50 -> -50 mm`、AB/整圈行驶时中心平衡、移动/任意位置保持。

- `VERIFIED（题目 PDF）`：水管角度须由机械执行器调节，但题目没有限定执行器必须为直流电机；MS42CG 步进方案不违反电机类型硬约束。
- 所有长度、速度、时间、角度统一为 mm、mm/s、ms、degree；步进命令单位为 command steps/s。
- NUCLEO-G491RE 是唯一实时控制器；MaixCAM2 只负责球位置视觉，比赛图传独立。
- `VERIFIED（用户/指导教师决策，2026-07-31）`：正式滚球为视觉单环 PID，只使用 MaixCAM2 真测量球位和由真帧导出的球速。SA100 不进入控制链、启动门禁或故障判断。
- `VERIFIED（用户授权，2026-07-31）`：机构有独立保护且可用行程充足，正式控制不依赖执行器中点或软件位置边界。执行器编码器仅用于堵转检测和调试；急停、复位、待机零输出仍保留。
- 左右轮必须保持两个独立轮速 PI；循迹只产生左右目标速度。

## 2. 2026-08-01 执行器迁移决策

原 MG513P60 + TB6612 直流执行器改为 MS42CG 两相步进电机 + 普菲德 TB6600 光耦型恒流驱动器。MS42CG 不能直接接 TB6612。

正式链路：

```text
MaixCAM2 status=1 球位/球速
 -> 视觉 PID（signed command steps/s）
 -> 步频/加速度/换向限制
 -> PB14 STEP、PC4 DIR、PC5 EN
 -> 外置步进驱动器 -> MS42CG
```

PB6/PB7 的 TIM4 编码器保留，但不进入 PID。PB14/TIM15 不再捕获 SA100，改为 STEP PWM；PA10 退出 TIM1_CH3 并固定低；PB1 作为旧 P60 STBY 安全锁固定低。TIM1 只保留左右轮 20 kHz PWM。

`VERIFIED（用户照片，2026-08-01）`：实物驱动器是普菲德 `TB6600`，外壳标注主电源 `DC 9–42 V`，信号端为 `ENA-/ENA+、DIR-/DIR+、PUL-/PUL+`，电机端为 `B-/B+/A-/A+`。当前采用 3.3 V 共阳接法：三组 `+` 接 NUCLEO 3V3，PB14/PC4/PC5 分别接 `PUL-/DIR-/ENA-`。ENA 是脱机输入，因此 PC5 低使光耦有效并脱机，PC5 高释放脱机并允许运行，正好符合现有复位低、逻辑高使能的软件语义。

`VERIFIED（用户台架实测，2026-08-01）`：3.3 V 共阳光耦输入在当前实物上工作可靠；正 `run` 命令使 MS42CG 顺时针旋转并抬高水管。沿该正转方向手转编码器一圈，TIM4 原始累计为 `-4096 count/rev`；配置使用 `APP_ENCODER_ACTUATOR_SIGN=-1`，因此遥测约定为正命令方向 `+4096 count/rev`。光耦各路电流的具体数值尚未提供。

`VERIFIED（用户拨码与受限机械点动，2026-08-01）`：用户已把 TB6600 的 SW1/SW2/SW3 设为 OFF/OFF/ON，即16细分、`3200 command pulses/rev`。切换后 `run 100 50`（约3.75脉冲）的机构位移显著小于切换前且无故障；正命令方向仍正确。极短反向命令未完全消除正向位移，符合当前机械回差/静摩擦特征，不能据此判定失步。台架 `run` 新增≤10命令脉冲预算，旧 `run 200 500` 将被拒绝。

仓库仍没有可唯一对应实物的官方 MS42CG 手册，也没有该普菲德驱动器的原厂电子手册，因此以下均为 `UNVERIFIED`：

- 电机额定电压/相电流、四线颜色与编码器线序；
- 3.3 V 光耦输入的具体电流数值、最终电流拨码；
- 编码器供电、输出类型/幅度，以及 counts/rev 的正反向重复性；
- 100 steps/s 闭环起始上限、225 steps/s 绝对上限、750 steps/s² 加速度和堵转阈值；
- 新视觉 PID `Kp/Ki/Kd/Ka=1.5/0/0.5/0` 及其最终输出上限。

这些值能编译不代表已调好。详细实测表见 `docs/measurements/ms42cg-stepper-migration-2026-08-01.md`。

## 3. 当前硬件分配

### 3.1 底盘

- 左/右 P30 直流减速电机继续共用一块双通道 TB6612。
- PA8/TIM1_CH1 左 PWM，PA9/TIM1_CH2 右 PWM，均约 20 kHz。
- PC0/PC1 左方向，PC2/PC3 右方向，PB0 共用 STBY，默认低。
- 左编码器 PA0/PA1 TIM2，右编码器 PA6/PA7 TIM3。
- `APP_ENCODER_LEFT_CPR/RIGHT_CPR=390` 与轮距 180 mm 仍为 `UNVERIFIED` 设计/旧工作值，必须在最终车体复测。

### 3.2 步进执行器

- PB14/TIM15_CH1：STEP，1 MHz 定时器基准，动态 ARR，50% 占空比。
- PC4：DIR。
- PC5：EN，当前软件假设高有效；复位/待机/故障为低。
- PB6/PB7 TIM4：执行器编码器 A/B，只用于堵转/调试。
- 执行器编码器实测为正命令方向原始 `-4096 count/rev`；集中反相后遥测为 `+4096 count/rev`。
- PA10、PB1：旧直流执行器控制，固定低，不接新驱动器。

### 3.3 视觉、循迹、显示与按键

- MaixCAM2 UART4：PC10 TX、PC11 RX；115200 8N1，RX 循环 DMA + IDLE。
- 视觉协议：`$B,<x_mm>,<status>\n`。`1` 是真测量，`2` 是保持值，`0` 无效。
- `status=2` 不刷新真测量时间、位置滤波或球速微分，不推进 PID/积分。
- 视觉坐标：左侧标线 `-100 mm`，中点 `0 mm`，右侧 `+100 mm`；球向图像右侧移动时数值增大。安装改变后必须重新验证。
- 八路数字循迹：PB10/PB11/PB12/PB13/PB15/PC6/PC7/PC12，默认低有效。
- OLED I2C1 PB8/PB9；IMU I2C3 PC8/PA11 预留，不进入正式控制。
- PA4/PA15 面板键输入上拉、按下接地；PC13 板载键为启动/急停。
- PA13/PA14 保留 SWD；PA2/PA3 保留 ST-Link VCP。

完整接头定位、DMA/NVIC 和接线合同见 `docs/stm32-pinout.md`。

## 4. 软件实现状态

- `VERIFIED（静态/编译，2026-08-01）`：`ST_Car.ioc`、`main.h`、GPIO/TIM 初始化、CMake 和 IAR 已迁移到步进引脚；GNU Arm 交叉编译以 `-Wall -Wextra -Werror` 成功链接。
- `motor` 只管理左右轮 TB6612；`stepper` 独立管理 STEP/DIR/EN。
- `encoder` 正确处理 TIM2 32 位与 TIM3/TIM4 16 位回绕；执行器不换算 mm/s。
- `control_loops` 保持左右轮独立 PI、循迹外环和视觉单环 PID；滚球 PID 输出 steps/s，不存在角度/位置内环。
- `safety_monitor` 使用实际 STEP 频率与 TIM4 计数判断执行器堵转；阈值未实物验证。
- 视觉超过 150 ms 无真测量时立即停 STEP；待机/复位/故障还会拉低 EN。
- 活动保持态 PID 输出为零时停 STEP，但 EN 可维持高以提供保持转矩。
- `bench_debug` 提供 `run <steps/s> <ms>`、视觉目标/序列、增益、步频上限、遥测与 PC13 急停；旧 `pulse/angle/sa cal/limit pwm` 命令已删除。
- `sa100.*` 仍可作为历史文件存在，但已退出 CMake/IAR 正式构建。不得重新加入控制链。

`VERIFIED（实物闭环，2026-08-01）`：MS42CG/TB6600 已完成上车、方向、编码器、视觉闭环和要求3硬件在环验证；相关原始日志与失败复盘见 `docs/measurements/visual-pid-2026-08-01.md`。

## 5. 控制与安全参数

集中定义在 `Core/Inc/app_config.h`。不得在算法中散落方向负号、细分、步数、限位或超时魔数。

- 控制快环 5 ms；循迹/状态 10 ms；遥测 100 ms。
- 视觉真测量控制保持 150 ms；通信新鲜度/启动检查窗口按配置执行。
- `VERIFIED（实物闭环，2026-08-01）`：正式要求3使用 `Kp/Ki/Kd/Ka=1.2/2.0/2.0/0`、常规步频上限 `400 steps/s`；端点释放为 `100 steps/s`。普通回中静摩擦释放后的短时捕获可放宽到 `2000 steps/s`，但该捕获状态在要求3序列中强制禁用。
- STEP 换向先停脉冲，DIR 建立 1 ms；视觉陈旧/故障立即停，不走减速斜坡。
- 堵转初值：实际频率绝对值至少 32 steps/s，连续 750 ms 编码器无变化；`UNVERIFIED`，必须用正常运动/人为堵转数据重定。
- 当前没有步进软件位置边界或自动回零，符合 2026-07-31 用户确认的机械保护决策。

## 6. 启动、模式与故障

状态为 `STANDBY`、`LEVELING`、`RUNNING`、`FINISHED`、`FAULT`。

- 循迹模式启动前必须检测到线。
- 滚球模式启动前必须取得新鲜 `status=1` 球位并满足对应起点容差。
- 执行器编码器不作为中点或位置启动门禁。
- 要求 3：首次进入 `+50 ±10 mm` 后切换到 `-50 mm`；最终按真实帧连续满足位置/速度 250 ms 后完成，随后继续保持和安全监控。
- `VERIFIED（实物连续两次，2026-08-01）`：同一固件逻辑下要求3分别用时 `4859 ms` 与 `3116 ms`，随后5秒保持分别约为 `-55.6 mm` 与 `-45.2 mm`；当前已满足5秒硬指标，但最坏时间余量仅141 ms，仍需继续提高重复性。
- 任何执行器堵转、丢线、阶段超时或启动检查失败均进入锁存故障；输出关闭后需回待机重新启动。
- PA4/PA15 短按仅在待机/调平态给相反方向的限时低步频试动；长按分别启动要求 3、保持当前真球位。

## 7. 强制实物调试顺序

任何输出测试前必须满足：车轮悬空、钢球移除；首次连杆脱开或无死点风险；驱动使能线隔离并下拉；全系统共地；电源极性/稳压/信号电平已测；可立即切断电机主电源。

按顺序验收并保存数据：

1. 驱动侧 EN/STBY 下拉，只测 PA8/PA9 20 kHz 和 PB14 STEP/PC4 DIR/PC5 EN 静态波形；
2. 左右直流电机仍按单电机低 PWM 验证；
3. 三个编码器手转验证方向、回绕和 counts/rev；
4. MS42CG 脱连杆、低步频短时，确认相线、方向、限流、细分、堵转特征和急停；
5. 固定小车验证视觉 `-100/0/+100 mm`、延迟、`status=2` 和丢球；
6. 空管、限时低步频确认机构正反方向、保持和急停；
7. 小车静止、最低步频上限完成视觉单环；
8. 完成要求 3 和任意位置保持；
9. 分别完成两轮速度环，再做低速循迹/A 点；
10. 最后低速组合并逐步提高；加速度项最后启用。

不得跳级，不得在编码器尚未证明有效时关闭堵转保护，不得靠注释宏绕过 EN、超时、急停或启动检查。

## 8. 电气接线原则

- MS42CG 只能接恒流双极步进驱动器的 A+/A-/B+/B-，不能接 TB6612。
- 四根电机相线颜色未确认；必须断电后用电阻档找出两个线圈对，再按该电机/驱动器手册确定极性。
- 主电源只进驱动器电源端，不接 NUCLEO 5V/3V3/GPIO。
- NUCLEO、驱动器、编码器和 MaixCAM2 必须共地；电机大电流回流不得穿过 MCU/传感器细地线。
- TB6600 的 `VCC/GND` 是照片标明的 DC 9–42 V 主电源端，绝不能接 NUCLEO 3V3/5V；NUCLEO 3V3 只接三个信号 `+` 端。
- 当前编码器接法已在手转计数测试中功能验证；供电、电平幅度和输出级的具体示波器数据仍须补录，未知 5 V 推挽信号仍禁止直接接 PB6/PB7。
- 驱动器 EN 和 STEP 建议在驱动侧加可靠下拉，使 MCU 复位/断电时禁用且无误步进。
- 相电流必须依据具体 MS42CG 铭牌/手册设置，并记录驱动器电流档位；不能把商品标题中的相似型号参数当实物额定值。

## 9. 仍需补齐的资料与实测

1. MS42CG 完整铭牌/官方手册、四相线与编码器线序；
2. 普菲德 TB6600 PCB 版本/原厂手册、3.3 V 输入电流具体数值与最终电流拨码；细分已设为16；
3. 电机相电流、供电范围、驱动器限流设置和温升；
4. 编码器供电、输出类型/电平，以及正反各三圈 counts/rev 重复性；方向和单圈计数已实测；
5. 最低可靠步频、最大安全步频/加速度、急停延迟和堵转阈值；
6. 新执行器下的视觉 PID 与要求 3/4/5/6 数据；
7. 左右 P30 counts/rev、最低 PWM、轮径/轮距和循迹整车参数；
8. 电源模块、线束、保险/断电开关和最终共地拓扑。

用户提供铭牌、购买链接、驱动器正反面照片或手册后，应先更新测量记录和本文，再修改对应 `UNVERIFIED` 参数。

## 10. 参考资料

- 题目 PDF：仓库题目原文件（比赛硬约束）。
- ST NUCLEO-G491RE 用户手册 UM2505：用于 Morpho/Arduino 接头映射。
- STM32G491 数据手册/参考手册：用于引脚复用、电平和 TIM15 能力。
- 具体 MS42CG 与驱动器手册：`TODO`，获得后必须归档或链接；商品页不得替代电气手册。

## 11. 2026-08-01 机械损坏事故与测试冻结

- `VERIFIED（用户现场报告）`：自动回中反复在水管已达右倾机械极限后继续驱动，机械结构已经损坏。此前“独立保护充分、可用行程充足”的结论已被本次实物事故推翻，禁止继续据此放开执行器位置边界。
- `VERIFIED（停机回读）`：事故后立即执行 `stop`，`sreq=0`、`sapplied=0`、三路电机 PWM 为零；事故前应用已进入 `FAULT_ACTUATOR_STALL`。回读为执行器编码器约 `-927 counts`、内部命令姿态约 `-1462 steps`、球位置约 `+107.5 mm`。原始记录：`tmp/mechanical-damage-immediate-stop-20260801.log`。
- 根因：自动回中只按球位置/速度切换，没有把已知机械端点、跨复位的累计执行器位移和运动方向设置为硬门禁；反复复位控制坐标后继续累积同向姿态。仅靠堵转超时不能防止撞限位和结构受损。
- `TODO / BLOCKED`：在操作者确认机械结构修复、手动置于安全中间姿态并重新测量左右端点前，停止一切烧录、复位和电机运动测试。恢复时必须先建立可跨任务使用的行程基准或实体限位开关，并验证方向相关限位能在脉冲发出前截断命令。
